ANTHROPIC_API_KEY=sk-ant-...

# Optional — for daily rate limiting (5 rewrites per IP per day).
# If unset, rate limiting is disabled (fine for local dev / low-traffic demo).
# Get a free Upstash Redis DB at https://console.upstash.com/
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
